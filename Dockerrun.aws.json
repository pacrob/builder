{
  "AWSEBDockerrunVersion": "1",
  "Image": {
    "Name": "${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com/${ECR_REPO_NAME}:${IMAGE_TAG}",
    "Update": "true"
  },
  "Ports": [
    {
      "ContainerPort": "${CONTAINER_PORT}"
    }
  ]
}
